# WebRAG dependencies — pinned to resolved stable versions as of 2026-03-04
# To update: uv pip install -r requirements.txt --upgrade, then freeze again

# API framework
fastapi==0.135.1
uvicorn[standard]==0.41.0

# Task queue
# Note: celery[redis] extra pins redis<6.0.0; redis 5.3.1 is the resolved compatible version
celery[redis]==5.5.2
redis==5.3.1

# Database (async engine for FastAPI + sync psycopg2 for Celery workers)
sqlalchemy==2.0.48
psycopg2-binary==2.9.11
asyncpg==0.31.0

# Vector store
qdrant-client==1.17.0

# Validation & settings
pydantic==2.12.5
pydantic-settings==2.13.1
python-dotenv==1.2.2

# Google AI — unified SDK (google-generativeai is officially deprecated)
google-genai==1.65.0

# Text processing
langchain-text-splitters==1.1.1
beautifulsoup4==4.14.3
lxml==6.0.2

# HTTP
requests==2.32.5